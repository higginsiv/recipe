version: 1
frontend:
  phases:
    preBuild:
      commands:
        - chmod 644 package-lock.json
        - npm i
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
